<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:android="http://schemas.android.com/apk/res/android">

      <item android:title="@string/add_to_favourites"
            android:icon="@drawable/ic_favorite"
            android:id="@+id/favouritesMenuItem"
            app:showAsAction="ifRoom"/>

      <item android:title="@string/edit_coffee"
            android:icon="@drawable/ic_edit"
            android:id="@+id/editMenuItem"
            app:showAsAction="ifRoom"/>

      <item android:title="@string/delete_coffee"
            android:icon="@drawable/ic_delete"
            android:id="@+id/deleteMenuItem"
            app:showAsAction="ifRoom"/>
</menu>
